const { defaultColormap, randomColormap } = require("./colormaps");
const { generateChartData } = require("./mockData");

module.exports = {
  defaultColormap,
  randomColormap,
  generateChartData,
};
